<view class="app news">
	<map
		style="width: 100%; height: 300px;"
		latitude="{{latitude}}"
		longitude="{{longitude}}"
		markers="{{markers}}"
		scale="18"
		covers="{{covers}}"
	>
	</map>
</view>
